import { ArrowLeft, Check, Clock3, ShieldCheck, X } from 'lucide-react'
import { useEffect, useState } from 'react'
import { decideApproval, getApprovals } from './automation-api.js'
import type { Approval } from './automation-model.js'
export function ApprovalInbox({storeId,onBack,onToast}:{storeId:string;onBack:()=>void;onToast:(message:string,kind?:'success'|'error'|'info')=>void}){const[items,setItems]=useState<readonly Approval[]>([]);const[loading,setLoading]=useState(true);const load=async()=>{setLoading(true);try{setItems(await getApprovals(storeId))}finally{setLoading(false)}};useEffect(()=>{void load()},[storeId]);const decide=async(item:Approval,decision:'approve'|'reject')=>{try{await decideApproval(storeId,item.id,decision);onToast(decision==='approve'?'Action approved. Automation is resuming.':'Action rejected. Run was cancelled.',decision==='approve'?'success':'info');void load()}catch(reason:unknown){onToast(reason instanceof Error?reason.message:'Approval could not be updated.','error')}};return <div className="automation-subpage"><button className="automation-back" onClick={onBack}><ArrowLeft size={16}/> Automations</button><header className="automation-section-header"><div><span className="automation-eyebrow">SAFETY CENTER</span><h1>Approval inbox</h1><p>Review the exact action before an automation changes customer communication or pricing.</p></div></header>{loading?<div className="automation-skeleton-page"><div className="automation-skeleton"/><div className="automation-skeleton"/></div>:items.length===0?<div className="automation-empty compact"><ShieldCheck size={30}/><h2>No actions waiting for approval</h2><p>When an automation needs your decision, it will appear here.</p></div>:<div className="approval-list">{items.map(item=><article key={item.id}><span className={`approval-risk ${item.riskLevel.toLowerCase()}`}>{item.riskLevel} RISK</span><h3>{item.workflowName}</h3><strong>{item.actionType.replaceAll('_',' ')}</strong><p>{item.preview}</p><div className="approval-expiry"><Clock3 size={14}/> Expires {new Date(item.expiresAt).toLocaleString()}</div><footer><button className="automation-secondary danger" onClick={()=>void decide(item,'reject')}><X size={15}/> Reject</button><button className="automation-primary" onClick={()=>void decide(item,'approve')}><Check size={15}/> Approve action</button></footer></article>)}</div>}</div>}
